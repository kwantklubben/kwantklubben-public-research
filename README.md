# Kwant Klubben — public projects

This repository contains projects explicitly approved for public publication by Kwant Klubben.

The working research repository is private. A project appears here only after a maintainer marks it `public: true` and runs the publish workflow.

## Project format

Each project preserves its project folder and README. `project.json` is the small public metadata file used by the website.
